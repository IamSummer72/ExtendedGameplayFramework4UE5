// Fill out your copyright notice in the Description page of Project Settings.

#include "EBExBarrel.h"
#include "Net/UnrealNetwork.h"

/*

#define REPOWNERONLY false

void UEBExBarrel::Reload(TSubclassOf<class AEBBullet> AmmoType)
{
}

void UEBBarrel::GetLifetimeReplicatedProps(TArray< FLifetimeProperty >& OutLifetimeProps) const
{
	Super::GetLifetimeReplicatedProps(OutLifetimeProps);

#if REPOWNERONLY
	DOREPLIFETIME_CONDITION(UEBExBarrel, bSpareAmmoUnlimited, COND_OwnerOnly);
	DOREPLIFETIME_CONDITION(UEBExBarrel, SpareAmmoCount, COND_OwnerOnly);
	DOREPLIFETIME_CONDITION(UEBExBarrel, CycleAmmoMax, COND_OwnerOnly);
#else
	DOREPLIFETIME(UEBExBarrel, bSpareAmmoUnlimited);
	DOREPLIFETIME(UEBExBarrel, SpareAmmoCount);
	DOREPLIFETIME(UEBExBarrel, CycleAmmoMax);
#endif

} */
